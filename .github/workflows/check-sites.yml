name: Checar sites monitorados

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Chamar endpoint de checagem
        run: |
          curl -sf --connect-timeout 10 --max-time 60 -X GET "https://hotmart-dashboard-woad.vercel.app/api/cron/check-sites" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
